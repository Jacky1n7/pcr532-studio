# Changelog

## 0.2.0-alpha.3 — 2026-09-06

新增纯 Rust Crypto1 运算、32 位状态恢复、PN532 原始字节/加密奇偶位交换、随机数诊断及实验性普通 nested。加入 Fudan 固定加密随机数卡的本地诊断读取和跨扇区复用密钥恢复。候选必须通过实卡认证，成功后可重读归档；加入取消、搜索时限和候选验证上限。实卡验证从字典读取 56/64 块提升到恢复并以普通密钥重读 64/64 块、32/32 个 A/B 密钥通过认证。命令行读取按识别容量操作。打开读卡器时重置标准射频帧设置，便于取消或失败后的重连。

仓库公开；仅继续迁移无需注册和充值的本地功能。借鉴 GPL-2.0-or-later 的 mfoc/Crapto1 和 GPL-3.0 的 Proxmark3 公开实现，组合应用从本版本起以 GPL-3.0-or-later 发布，保留原 MIT 及上游署名。hardnested、DarkSide 和厂商专有写卡迁移仍未完成。

## 0.2.0-alpha.2 — 2026-09-06

修复 CI 并行测试中的临时目录重名：命名加入进程内原子序号，同时改进备份临时文件和归档命名；失败清理仅删除本次成功创建的临时文件。新增 8000 个并发名称的唯一性测试。CI 只针对 main 与 PR 构建，避免同一提交因标签推送重复构建。

## 0.2.0-alpha.1 — 2026-09-06

首次 Rust 预览版：替换 Python/Qt 界面与 C/libnfc 读写桥，使用 Rust 直接管理串口、卡片数据、NDEF、后台任务和 Mac 打包。兼容旧版 JSON 文档。实机读取结果与旧版一致，为 56/64 块。

已知差异：尚未迁移旧版 C 后端提供的 nested/hardnested/DarkSide、Gen1a UID 写入。Windows 专有功能仍有缺口，见迁移状态与 Issues。

此前 Python 0.1 原型仅保留在本机，不作为 Rust 仓库或 Release 的组成部分。
